# LLM
LLM_PROVIDER=openai
LLM_MODEL=gpt-4.1
AGGREGATE_MODEL=gpt-4o-mini
OPENAI_API_KEY=
# Optional: route OpenAI calls to a compatible endpoint
# OPENAI_BASE_URL=

# Inference
NUM_INFERENCE=10
ADD_REFERENCES=true

# PubMed (only required if ADD_REFERENCES=true)
PUBMED_EMAIL=
PUBMED_API_KEY=

# Server
HOST=0.0.0.0
PORT=8000
